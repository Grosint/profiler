[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv || python -m venv /opt/venv",
  "/opt/venv/bin/pip install --no-cache-dir --upgrade pip setuptools wheel",
  "/opt/venv/bin/pip install --no-cache-dir cython numpy scipy",
  "/opt/venv/bin/pip install --no-cache-dir -r requirements.txt",
  "/opt/venv/bin/playwright install chromium"
]

[phases.build]
cmds = []

# Note: Start command is defined in Procfile, not here
# Railway will automatically detect and use the Procfile
